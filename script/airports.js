class Airport {
    constructor(name, map, imageBounds, positions, exits, taxyways, holdpoints, runways) {
        this.name = name;
		this.map = map:
		this.imageBounds = imageBounds;
        this.positions = positions;
        this.exits = exits;
        this.taxyways = taxyways;
        this.holdpoints = holdpoints;
        this.runways = runways;
    }
}